<div class="main-mini-menu-bar">
    <div class="container">
        <div class="row">
            <% if (isRealmSelected) { %>
            <div class="col-xs-12 text-center">
                <div class="main-mini-menu-bar-title" data-id="dashboard"><i class="fa fa-tachometer"></i> <%= t('Dashboard') %></div>
                <div class="main-mini-menu-bar-title" data-id="rent"><i class="fa fa-money"></i> <%= t('Rents') %></div>
                <div class="main-mini-menu-bar-title" data-id="occupant"><i class="fa fa-group"></i> <%= t('Tenants') %></div>
                <div class="main-mini-menu-bar-title" data-id="property"><i class="fa fa-key"></i> <%= t('Properties') %></div>
                <div class="main-mini-menu-bar-title" data-id="owner"><i class="fa fa-user"></i> <%= t('Landloard') %></div>
            </div>
            <% } else { %>
            <div class="col-xs-12">
                <div class="clearfix">
                    <div class="pull-left">
                        <div class="main-mini-menu-bar-brand">
                            <a href="/"><%=config.website.product.name%></a>
                        </div>
                    </div>
                    <% if (!isLogged) { %>
                    <a class="pull-right btn btn-primary" href="/login" role="button"><i class="fa fa-sign-in"></i> <%= t('Sign in') %></a>
                    <% } %>
                </div>
            </div>
            <% } %>
        </div>
    </div>
</div>
<% if (isLogged) { %>
<a href="#" title="Menu principal" class="main-menu-button" data-id="show-main-card"><i class="fa fa-bars"></i></a>
<div class="main-menu-card">
    <h2 class="page-header"><%=config.website.product.name%></h2>
    <ul class="nav">
    <% if (isRealmSelected) { %>
        <li><a href="#" class="nav-action" data-id="dashboard"><i class="fa fa-tachometer"></i> <%= t('Dashboard') %></a></li>
        <li><a href="#" class="nav-action" data-id="rent"><i class="fa fa-money"></i> <%= t('Rents') %></a></li>
        <li><a href="#" class="nav-action" data-id="occupant"><i class="fa fa-group"></i> <%= t('Tenants') %></a></li>
        <li><a href="#" class="nav-action" data-id="property"><i class="fa fa-key"></i> <%= t('Properties') %></a></li>
        <li><a href="#" class="nav-action" data-id="owner"><i class="fa fa-user"></i> <%= t('Landloard') %></a></li>
        <li><a href="/selectrealm"><i class="fa fa-building-o"></i> <%= t('Organizations') %></a></li>
    <% } %>
        <li><a href="/logout" class=""><i class="fa fa-sign-out"></i> <%= t('Sign out') %></a></li>
    </ul>
</div>
<% } %>
<div class="main-menu-bar">
    <div class="navbar navbar-default navbar-fixed-top" role="navigation">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="/" title="<%=config.website.product.slogan%>"><%=config.website.product.name%></a>
            </div>
            <div class="collapse navbar-collapse">
                <% if (isRealmSelected) { %>
                <ul class="nav navbar-nav">
                    <li><a class="nav-action" data-id="dashboard" href="#"><i class="fa fa-tachometer"></i> <%= t('Dashboard') %></a></li>
                    <li><a class="nav-action" data-id="rent" href="#"><i class="fa fa-money"></i> <%= t('Rents') %></a></li>
                    <li><a class="nav-action" data-id="occupant" href="#"><i class="fa fa-group"></i> <%= t('Tenants') %></a></li>
                    <li><a class="nav-action" data-id="property" href="#"><i class="fa fa-key"></i> <%= t('Properties') %></a></li>
                    <li><a class="nav-action" data-id="owner" href="#"><i class="fa fa-user"></i> <%= t('Landloard') %></a></li>
                </ul>
                <% } %>
                <% if (isRealmSelected) { %>
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        <% if (isMultipleRealmsAvailable) { %>
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown"> <%= account.firstname + ' ' + account.lastname %> <%if (!isDefaultRealmSelected) {%>@<%= account.realm.name %> <%}%><span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <% for (index=0; index<account.realms.length; index++) { %>
                                <li>
                                    <a href="#" class='realm-action' data-id="<%=account.realms[index]._id%>"><i class="fa fa-building-o"></i> <%= account.realms[index].name %> </a>
                                </li>
                            <% } %>
                            <!--li><a href="/selectrealm"><i class="fa fa-plus"></i> <%= t('New organization') %></a></li>
                            <li role="separator" class="divider"></li-->
                       
                            <li><a href="/logout"><i class="fa fa-sign-out"></i> <%= t('Sign out') %></a></li>
                        </ul>
                        <% } else { %>
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown"> <%= account.firstname + ' ' + account.lastname %><span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="/logout"><i class="fa fa-sign-out"></i> <%= t('Sign out') %></a></li>
                        </ul>
                        <% } %>
                    </li>
                </ul>
                <% } else if (isLogged) { %>
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown"> <%= account.firstname + ' ' + account.lastname %><span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="/logout"><i class="fa fa-sign-out"></i> <%= t('Sign out') %></a></li>
                        </ul>
                    </li>
                </ul>
                <% } else { %>
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="/login"><i class="fa fa-sign-in"></i> <%= t('Sign in') %></a></li>
                </ul>
                <% } %>
            </div><!--/.nav-collapse -->
        </div>
    </div>
</div>